# SSL Check plugin for ExpressionEngine 2

Check whether page is accessed over secure (https) connection


{exp:ssl_check}
connection is secure
{if no_results}
non-secure connection
{/if}
{exp:ssl_check}
